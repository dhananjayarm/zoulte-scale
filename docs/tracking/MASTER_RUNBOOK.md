# MASTER RUNBOOK — Zoulte Scale

Append-only chronological record. One entry per completed step. Never edit prior lines.

Format: `[date] [step-id] — what was done. Tests: +N added, M/M passing.`

---

[2026-07-27] PLAN — docs/IMPLEMENTATION_PLAN.md written and agreed: rework in-repo (D-1), no @zoulte/shell (D-2), offline unlock (D-3), borrow zoulte-pos-ui offline backbone (D-4), device-only weights (D-5), void-never-delete (D-6), exception-based online verification (D-7), sync⊥verification (D-8), thin masters over xaur-core + default category (D-9), independent product / SFA-today-OMS-later via relocatable module (D-10). Phases 0–8 defined.
[2026-07-27] P0-groundwork — authInterceptor (Bearer attach + 401→logout w/ ?expired=1, /authenticate exempt both directions) wired via provideHttpClient(withInterceptors); HttpGet/HttpPost stripped of per-service auth headers (postPublic/getPublic removed, login now posts via interceptor-exempt path); services modernised to inject(). Serial parsing extracted to pure scale-reading.parser.ts (parseWeightLine, StabilityTracker, LineBreakTransformer) — ScaleSerialService now port-handling only. styles.css gains theme-independent status tokens + .chip component (sync/stability/verification states). docs/tracking adopted. Tests: +13 (parser/stability/line-split), 16/16 passing. Build clean.
[2026-07-27] P1-electron — Electron shell live: electron/{main,preload,ipc,station-config}.js + db/{schema.sql,migrations,connection,datastore}.js (POS pattern; bridge = window.scaleBridge, contextIsolation on, renderer never touches Node). Full ws_* baseline schema shipped as migration v1 (reading/balance_check/audit/outbox/user_cache/product+category_cache/sync_meta/config — 9 tables). Serial auto-grant: select-serial-port handler picks pinned station-config device else first port (Electron has no built-in picker). Stack pinned electron@41.7.0 + better-sqlite3@12.10.0 (exact), abi:node/abi:electron scripts; --validate mode green (ABI 145, 9 tables). build:desktop (base-href ./) verified; .gitignore covers *.db + /release. electron-builder installer deferred to Phase 8 (BACKLOG). Tests: 16/16 unchanged.
[2026-07-27] P2-datalayer — Local-first capture pipeline live. Renderer: DataStore iface + IpcDataStore over scaleBridge (data/), uuid/nowIso ids; transactional outbox (outboxOp UPSERT-coalesce) + SqliteOutboxGateway (claim/markSynced-with-mirror/markRetrying/markDeadLetter/recoverStuck/counts/listOpen/requeue); SyncEngine as PURE class over OutboxGateway+CloudClient (deliberate deviation from POS SQL-in-engine — unit-testable in Karma); HttpCloudClient maps READING→POST api/weightscaleproduct (4xx=REJECTED terminal, else throw=retryable; SUPPORTED_ENTITY_TYPES gate keeps future types queued); ConnectivityService (navigator.onLine signal); SyncService (drain tick 10s + online-event trigger + recoverStuck on start, pending/failed signals). ReadingStore port: LocalReadingStore (reading+audit+outbox ONE transaction, drain nudge) vs RemoteReadingStore (browser = original direct-HTTP), factory-provided in app.config. Weighing screen rewired to store + Sync chip column. Header: app-sync-status child (Online/Offline + Scale + sync badge chips, outbox panel w/ per-row status, error, Retry, Sync-now). run-in-electron harness + outbox.test.js (atomicity/rollback/coalesce/recover/mirror) green. Tests: +6 Karma (engine/backoff) = 22/22, +1 Electron integration. Build clean.
[2026-07-27] P3-offline-unlock — Offline unlock live (D-3): password-hash.ts (PBKDF2-SHA256 verifier, POS pin.ts port, constant-time verify); OfflineUnlockService (cacheCredentials on every successful ONLINE login via AuthService tap; tryUnlock verifies ws_user_cache, 5-attempt in-session lockout, previously-seen users only; sets same localStorage keys as real login minus token + offline-session flag/signal); login screen: offline→unlock directly, online-fail status 0→fallback unlock, distinct messages (unknown-user needs online first / locked / invalid), capture-only confirmation banner, ?expired=1 session-expired banner (F-003 🟢); onlineOnlyGuard bounces reports routes → /weightscale during offline sessions; header shows "Capture only" chip; logout ends offline session. Tests: +4 (password-hash) = 26/26. Build clean.
[2026-07-27] P5-weighing-redesign — Weighing screen rebuilt as an instrument face (Phase 4 masters skipped for now — backend-blocked F-002; session card degrades to free-text until product master lands, picker slots in later). Session model: WeighingSession set once via Start-session (form → locked summary card w/ product/batch chips, mfg/exp meta, live capture COUNT, Change-product/batch); captures record against the session, not the form. Hero readout: giant tabular-nums weight (green when stable), Stable/Settling…/No-reading chip, compact connect controls (baud picker only when disconnected), scale-disconnected warn banner, big Capture button gated session+connected+STABLE with a one-line captureHint explaining exactly what blocks it. Recent-captures strip (last 10, session-scoped feel): weight/product·batch/time/sync chip + Void → inline reason-required confirm (LocalReadingStore.voidReading: status=VOID + attribution + READING_VOIDED audit + VOID outbox event in one txn; hidden in browser, supportsVoid=false). Full history/search removed from screen (Reports own it). OnPush + inject() + computed throughout. Tests: 26/26. Build clean.
[2026-07-28] P5-reconnect — Scale-loss handling completed (closes the Phase 5 auto-reconnect promise): ScaleSerialService listens to navigator.serial connect/disconnect events — cable pull/power-off tears down cleanly (state disconnected, latestReading CLEARED so no frozen weight can be trusted, stability reset); device reappearing auto-reconnects with the last options when the session owned a port (manual Disconnect opts out via autoReconnect flag). Header scale chip gains distinct danger state for 'error'. BACKLOG: idempotent-ingest demoted from URGENT to ride with first backend batch (owner decision; F-004 stays monitored). Tests: 26/26. Build clean.
[2026-07-28] DEV-retry — electron:dev blank-screen fixed: did-fail-load → retry devUrl every 1.5s until the Angular dev server answers (start order no longer matters).
[2026-07-28] P4-masters-core — Product/category Setup screens live (scoped Phase 4). Investigation (agent over xaur_oms_web feature/material + zoulte-catalog-engine + xaur-core): two backend surfaces exist for the same cr_material_master / cr_material_category_master tables — new /cat/api (catalog engine, separate service :3033, NOT reachable via sfa/oms prefixes) vs legacy xaur-core @CoreController endpoints (/sf/api/cr/inv/* — live on our backend TODAY). Decision: build on legacy core endpoints via existing base URL; collect the UNION of required fields (category: code/name/defaultUom/invTypeCode; material: code/name/categoryCode/invTypeCode) so payloads stay valid when flipping to /cat later — all paths isolated in material-api.service.ts (one-file switch). Screens: /setup (authGuard+onlineOnlyGuard) with SetupFacade (forkJoin lists, soft-fail per stream, gram-family + FG defaults) + product-card (name+category visible; code auto-derived w/ editable Details toggle for code/uom/invType; last-used category remembered; "create a category first" guard) + category-card (single name field, code/uom/invType auto). Base primitives (.card/.field/.btn/.banner/.mono) promoted to global styles.css. Sidebar: client-side Administration→Setup group appended until server menu config exists (BACKLOG). Tests: 26/26. Build clean.
[2026-07-28] P5-fonts — Typography fixed (blank font-family fell back to browser serif after the redesign dropped the old per-page stack): DM Sans 400/500/600/700 + DM Mono 400/500 TTFs copied from zoulte-pos-ui (offline-bundled, no CDN — station renders identically with zero network), fonts.css added to angular.json styles (build+test); --font-ui/--font-mono tokens in styles.css, body+form-controls on DM Sans, figures (hero readout, recent weights/times, session count) on DM Mono tabular. Build clean.
[2026-07-28] MENU+CHPW — Module code fixed as WT-SCL (owner; replaces xaur-scale). docs/sql/menu-inserts-wt-scl.sql: mst_menu_master + mst_menu_access seed (access rows REQUIRED — menuaccess query inner-joins them) for Operations (Weighing Station ✓, Verification inactive, Daily Balance Check inactive) / Reports (landing + Product/Expiry REPORT rows) / Setup (Users inactive, Category ✓, Product ✓, Employee inactive) under roles WS_OPERATOR/WS_VERIFIER/WS_ADMIN + commented dev-grant block; isactive=0 rows flip live as phases ship. Frontend: MENU_MODULE→WT-SCL; sidebar fallback now keyed on server /setup links (header 'Setup'); routes /setup/category + /setup/product alias SetupComponent. Login change-password provision: AuthService.changePassword→POST chpw?oldpwd&newpwd (authenticated, SFA-web contract); login card gains change mode (current+new+confirm, min 8, match+differ checks, online-only) — user-opened via link or FORCED by changepw flag in login response; success re-seeds offline-unlock cache with the new password then signs in. Tests: 26/26. Build clean.
[2026-07-28] MENU-filter — menuaccess returns module_code='ALL' platform rows from the shared dev DB (payload lacks module_code, so no module filter possible; server/data untouchable — shared). Fix: MenuService whitelists items to this app's fixed route prefixes (weightscale/verification/balance-check/reports/setup) — foreign 'ALL' noise dropped for both sidebar and report cards. New route = one prefix to add. Tests: 26/26. Build clean.
[2026-07-28] MENU-filter2 — Owner asked to also drop moduleCode:null rows; NOT implementable as asked — core's mappers never select module_code, so EVERY payload row (ours included) is moduleCode:null and that filter would blank the menu. Actual fix for the remaining leakage (foreign ALL rows whose links share our prefixes, e.g. other products' reports/setup): whitelist tightened from prefix-match to the EXACT closed set of this app's menu links (mirrors menu-inserts-wt-scl.sql; new menu row = one line in APP_MENU_LINKS). Tests: 26/26. Build clean.
[2026-07-28] P4-split-pages — Combined /setup page split into dedicated routed screens matching the WT-SCL menu entries: /setup/category → CategorySetupComponent ("Material Categories", narrow) and /setup/product → ProductSetupComponent ("Products", w/ Manage-categories cross-link); /setup now redirects to setup/product; retired setup.component.* deleted. Cards + SetupFacade unchanged (each page provides its own facade instance); shared setup-page.css. Tests: 26/26. Build clean.
[2026-07-28] P4-list-fix — Category GET returned empty after successful POST: /materialcategorys hides categories whose inv type isn't is_purchaseable (our FG default is saleable-not-purchaseable — hidden filter via tb_inventory_type subquery). Switched listCategories → /productcategorys/company (findByCompanyCode — company-only, no inv-type/division conditions). Products list risk logged: ALL core material list endpoints query the vw_products DB VIEW with EXACT division_code match (null division ⇒ no rows; view may also require price/category joins; view may not even exist in the SFA schema) — F-005. Tests: 26/26. Build clean.
[2026-07-28] P4-names-only — Codes removed from the Setup UI (owner direction): category list shows names only; product table drops the Code column and maps Category codes→names; "Will save as CODE" preview lines and the code-override field in Details removed. Codes still auto-derive from names for the payloads (deriveCode) — duplicate-name collisions surface as the server's duplicate-code error. Tests: 26/26. Build clean.
[2026-07-28] P4-legacy-dto — joint.jpg triage, three bugs: (1) "Organization Setup" = foreign ALL-module row linking exactly '/setup' → passed whitelist; bare 'setup' removed from APP_MENU_LINKS (our rows only use setup/* sub-links; client fallback + redirect unaffected). (2) Dual sidebar highlight = that row's '/setup' prefix-matching '/setup/product' via routerLinkActive — gone with (1). (3) Blank "Existing (1)" product row = OUR create: legacy MaterialMasterDTO speaks productName/productCode/productCategory, not the catalog-engine materialName/materialCode names sent — POST 200'd with NULL name. material-api.service now translates BOTH ways at the API boundary (app keeps catalog-engine names internally; /cat switch later deletes the mappings, not callers). F-005 partially confirmed: /products DOES return rows (vw_products exists, division matches) — field naming was the visible failure. DB cleanup needed for the null-name row. Tests: 26/26. Build clean.
